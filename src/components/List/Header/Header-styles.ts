import { HeaderStyles } from './Header-types';

export const headerStyles: HeaderStyles = {
  defaultStyles: {
    labelStyle: {
      opacity: 0.8,
      marginBottom: 16,
      textAlign: 'center',
    },
  },
};
